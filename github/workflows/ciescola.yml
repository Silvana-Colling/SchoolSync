name: CI SchoolSync
on: [push]
jobs: 
  build:
    runs-on: ubuntu-latest 
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v4
      whit:
        java-version: '17'
        distribution: 'temurin'
    - run: javac *.java